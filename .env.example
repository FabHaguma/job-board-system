# Environment variables for production deployment
# Copy this file to .env and update the values

# Backend Configuration
JWT_SECRET=your_super_secret_key_that_is_very_long_and_random_change_this_in_production
NODE_ENV=production
PORT=3001

# Database Configuration
# SQLite database will be stored in the persistent volume

# Security Notes:
# 1. Generate a strong JWT secret with at least 32 characters
# 2. Use a secure password generator for production secrets
# 3. Never commit the actual .env file to version control
